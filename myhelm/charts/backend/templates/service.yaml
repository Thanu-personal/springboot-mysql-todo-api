apiVersion: v1
kind: Service
metadata:
  name: svc-todoapp
spec:
  selector:
    app: todoapp
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081
  type: NodePort