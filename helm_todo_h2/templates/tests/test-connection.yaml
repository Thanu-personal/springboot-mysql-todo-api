apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm_todo_h2.fullname" . }}-test-connection"
  labels:
    {{- include "helm_todo_h2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm_todo_h2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
